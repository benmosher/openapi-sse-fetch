[project]
name = "echo-chat-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard] @ git+https://github.com/benmosher/fastapi.git@feature/generic-sse-data",
]

[tool.fastapi]
entrypoint = "main:app"
